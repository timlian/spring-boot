buildscript {
  repositories {
    jcenter()
  }
  
  dependencies {
    classpath 'org.springframework.boot:spring-boot-gradle-plugin:+'
  }
}

repositories {
  jcenter()
}

project.apply(plugin: spring-boot)
